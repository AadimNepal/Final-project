<!DOCTYPE html>
<html>
<head>
    <title>Patient Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 p-6">
    <div class="max-w-4xl mx-auto">
        <div class="flex justify-between items-center mb-6">
            <h1 class="text-3xl font-bold text-blue-600">Welcome, {{patientName}}</h1>
            <a href="/logout" class="bg-red-500 text-white px-4 py-2 rounded hover:bg-red-600">Logout</a>
        </div>

        <div class="bg-white rounded-lg shadow-md p-6">
            <h2 class="text-xl font-semibold text-gray-700 mb-4">Your Healthcare Providers</h2>
            
            {{#if doctors}}
            <div class="space-y-4">
                {{#each doctors}}
                <div class="border rounded-lg p-4 hover:bg-gray-50 transition duration-150">
                    <a href="/patient-dashboard/doctor/{{id}}" class="block">
                        <div class="flex justify-between items-center">
                            <div>
                                <h3 class="text-lg font-medium text-blue-600">Dr. {{this.name}}</h3>
                                <p class="text-sm text-gray-600">
                                    Total Encounters: {{encounterCount}}
                                </p>
                            </div>
                            <div class="text-sm text-gray-500">
                                Last Visit: {{formatDate lastVisit}}
                            </div>
                        </div>
                    </a>
                </div>
                {{/each}}
            </div>
            {{else}}
            <p class="text-gray-500">No encounters recorded yet.</p>
            {{/if}}
        </div>
    </div>
</body>
</html>